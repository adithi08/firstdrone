#Drone CI File! NS-ASA
#Commit Message: ASA-Drone Server

kind: pipeline
type: docker
name: default

steps:

- name: build
  image: golang:1.13
  commands:
    - "go build -o ./myapp"

- name: publish  
  image: plugins/docker
  settings:
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    repo: adithi08/d-adithi
    auto_tag: true
    auto_tag_suffix: drone-build

#Will be adding steps for Container image scan, showing demo to James Robinson
