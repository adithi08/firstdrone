#Drone CI File! NS-ASA
#Commit Message: ASA-Drone Server

kind: pipeline
type: docker
name: default

steps:

- name: build
  image: golang:1.13
  commands:
    - "go build -o ./myapp"


#- name: statictest
#  image: zaquestion/sonarqube-scanner
#  pull: if-not-exists
#  commands:
#    - zaquestion/sonarqube-scanner

- name: code-analysis
  image: aosapps/drone-sonar-plugin
  settings:
      sonar_host:
        from_secret: http://34.230.90.243:9000/
      sonar_token:
        from_secret: a24ea0b7edb941ff3cbb85d68f156d2ab5a3416f


- name: publish  
  image: plugins/docker
  settings:
    username:
      from_secret: docker_username
    password:
      from_secret: docker_password
    repo: adithi08/d-adithi
    auto_tag: true
    auto_tag_suffix: drone-build

#Will be adding steps for Container image scan, showing demo to Security Team and Chris Y
